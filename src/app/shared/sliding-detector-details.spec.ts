import { SlidingDetectorDetails } from './sliding-detector-details';

describe('SlidingDetectorDetails', () => {
  it('should create an instance', () => {
    expect(new SlidingDetectorDetails()).toBeTruthy();
  });
});
